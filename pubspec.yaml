name: share_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

# ✅ YAHI WOH LINE HAI JISSE ERROR FIX HOGA
environment:
  sdk: '>=3.0.0 <4.0.0' 

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true